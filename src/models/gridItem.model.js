class GridItem {
  constructor(type, id, text, subText, color, imgUrl, linkUrl) {
    this.type = type
    this.id = id
    this.text = text
    this.subText = subText
    this.color = color
    this.imgUrl = imgUrl
    this.linkUrl = linkUrl
  }
}
export default GridItem
